'use strict'

let hasDriversLicence = false;
let passTest = true;

if (passTest) hasDriverLicence = true; // due to strict mode it gives us visible error in console
if (hasDriversLicence)console.log('I can Drive');

//strict mode restricts us to do some-thing that is not required